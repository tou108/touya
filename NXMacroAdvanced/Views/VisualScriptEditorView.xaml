<UserControl x:Class="NXMacroAdvanced.Views.VisualScriptEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{StaticResource NxBg}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <md:PackIcon Kind="VectorPolyline" Width="64" Height="64"
                         HorizontalAlignment="Center"
                         Foreground="{StaticResource NxPrimary}" Opacity="0.5"/>
            <TextBlock Text="ビジュアルスクリプトエディター"
                       FontSize="20" FontWeight="Medium"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       HorizontalAlignment="Center" Margin="0,16,0,8"/>
            <TextBlock Text="この機能は現在開発中です。"
                       FontSize="14"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <TextBlock Text="マクロエディタータブでテキスト形式のスクリプトをご利用ください。"
                       FontSize="12"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       HorizontalAlignment="Center" Opacity="0.6"/>
        </StackPanel>
    </Grid>
</UserControl>
